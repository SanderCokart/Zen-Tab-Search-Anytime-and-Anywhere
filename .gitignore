# Upstream clone kept locally for reference (code lives in entrypoints/)
Zen-Tab-Search/

node_modules/
.output/
.wxt/
signed/
web-ext-artifacts/
*.xpi
dist/

# dotenvx: plaintext env (generated by npm run env:use:local)
.env

# dotenvx private decryption key (never commit)
.env.keys

# Accidental non-Firefox build output
.output/chrome-mv3/
